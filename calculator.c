#include <stdio.h>
int calculate(int a, int b) {
    // Some calculation
    return a * b;
}
#include <stdio.h>

int calculate(int a, int b) {
	// A different calculation
	return a + b;
}
#include <stdio.h>

int calculate(int a, int b) {
        // A different calculation
        return a + b;
}
